# DEVIATIONS from STORYBOARD.md (one line each, with the reason)

- section 6 numbers: the final render uses the verified `research/results/movie_facts.json` (the merged 1x1..5x5 exhaustive sweeps, 4,730,000 random boxes up to 512x512, 3,484,889 adversarial runs), so the section-6 rules give LONGEST_ONSET = 1,323,594 (random k=512, p=0.5, seed 20260921, sample 9269; 130,511 black cells; re-simulated and asserted in-render) and TOTAL_TESTED = 41,835,387 (= `total_runs_all_experiments`, cross-checked; the loader aborts on a mismatch). The storyboard's "expected" 466,066 / 196,974 predate those results. `facts_provisional.py` remains only as the fallback for the case that the verified file is absent.
- section 6 schema: the verified file does not use the section-6 field names; `common.adapt_verified_schema` maps them explicitly (`exhaustive.kN` -> `N`, `n_cap` -> `cap_hits`, `exhaustive.4.histogram_log_bins` and `k4_records_csv` read from the result file named by `exhaustive.source` as section 5 says, `random.longest_config` = the (k, p, seed, sample_index, n_black) tuple parsed from `random.longest_config_summary` and regenerated with the exact randexp splitmix64, `random.max_k` = the largest k of any random sweep table, `adversarial.n_tested` <- `total_runs`). Nothing is invented; a field that stays missing still aborts by name.
- section 6 `{LONGEST_SOURCE}`: caption format is `index 9269 · 512x512 · p=0.5` for a random source (the sample index, not the seed; the storyboard example said "seed 4491"), `evolved · 12x12 box · 11 cells` for an adversarial source and `KxK box · pattern N` for an exhaustive source. Exact onset ties prefer exhaustive > random > adversarial (a search that only re-found the 5x5 maximum, as attack B did, is not that configuration's origin).
- S12 card A (director's cut): the first-person `I evolved starts to stall it.` is gone; S12 shows the provably slowest 5x5 start (`exhaustive.k5.max_onset_config`, 11 cells, onset 233,232, re-simulated and asserted) instead of the 1,323,594-step 512x512 random box (an unreadable grey noise square): card A reads `The stubbornest 5x5 start:` (Regular 56) with the caption `11 cells · slowest of all 33,554,432` (Mono 36: it measures 953 px at the requested Mono 44 and 867 px at Mono 40, which left the clamped band no side padding - reviewer V3; 36 is the section-7 minimum for mono captions and keeps the 28 px padding), both numbers from the facts.
- S13 wording: line 1 reads `{TOTAL_TESTED_FMT} runs tested.` (not `starts tested.`) because the section-6 sum counts runs: the exhaustive boxes are nested (every 1x1..4x4 pattern is also a 5x5 pattern) and the adversarial count is hill-climb evaluations with repeats; the number of distinct starts is only known approximately, so the exact run count is shown with honest wording. Line 2 = certified runs, line 3 = the difference (0).
- section 2.4 measured widths: with the verified numbers the only text shrink is `41,835,387 runs tested.` (886 px at Mono 64, shrunk to 60; the three S13 lines share that size), logged in `build/timeline_full_shrinks.log`; S12 card B `Longest delay: 233,232 steps.` fits at Regular 56.
- section 4 / Appendix B (k5 certification basis): originally `exhaustive.json` k5 recorded only the sign of the per-period displacement; after the adversarial review the whole 1x1..5x5 enumeration was re-run under a strict, sufficient certificate that also checks the full displacement (`research/results/exhaustive_strict.json`), and DESCRIPTION.md says so.
- S07 turn strip: the 104-tick bracket, loop arrow and `104 turns` label are drawn BELOW the strip (y 1500-1548) instead of above it, because the counter at y = 1380 and the strip at y 1440-1500 leave no room above; everything stays inside the safe zone (y <= 1600).
- section 2.6 counters: `draw_counter(y=...)` treats y as the vertical centre of the digits with a compact 14 px band (not the 28 px card band), so the S07 counter at y = 1380 clears the strip at 1440.
- section 2.5 contrast cap in S05: with the exponential ramp solved for the provisional onset (9,977) the > 100 steps/frame threshold is crossed at frame 444 (t = 14.8 s; frame 443 draws 98 steps, frame 444 draws 101), not "~13.3 s"; the cap is applied from the frame the schedule actually exceeds 100 steps.
- section 4.1 oscillator: decimation 192 kHz -> 48 kHz uses a 4-sample box average followed by the 6 kHz one-pole lowpass (instead of lowpass-then-drop) so aliasing of the sample-and-hold signal is lower; audible result identical in the band of interest.
- S09: the pad thins to a lone A2 at −24 dBFS instead of literally "bare drone" (section 4.3 lists a lone A2 for S09); the 6 re-rolls are the pattern changes at 35.8, 36.1, …, 37.3 s (each a click cluster), the 6th change being the lock (knock) on the 7th pattern.
- S10 tile label: `road at step {TILE_ONSET_i}` is 457 px at Mono 40 and cannot fit a 360 px tile, so it is drawn as two lines (`road at` / `step 19,039`) on a compact band inside the tile (placed off the road, see the S10 tile label line of fixer round 2).
- S10 tile camera: centred on the centroid of the cells modified up to the onset (lagged 0.3 s) and held afterwards, so the road exits the tile as the storyboard describes; following the centroid of all modified cells would drag the view along the road.
- S10 certification: re-derived in-render as max(onset + 21 periods, first step with the ant >= 20 cells beyond the pre-onset bbox in both coordinates); agrees with k4_records.csv `steps` to within one period (41.7 / 41.1 / 40.7 / 40.2 / 40.4 / 40.5 s as in 3.3). The pad under card A is the open A-major chord (unspecified), lone A2 under card B as specified.
- S11: a pure exponential fill 1 -> 2,000 dots/frame sums to ~28k in 105 frames, so the rate is a power ramp 1 -> 2,000 solved to complete exactly on the 47.5 s frame. The counter and side bars sit directly on the plain background (no band): a banded counter at y = 390 collides with the card (ends y ~381) and the wall (y 470), so y = 390 is the counter's text top. The side bars start at x = 136 (see the S11 side-bar line below for the four-row layout). The box size in `up to 512x512` is the facts field `random.max_k` (written by facts_provisional.py; derived from the sweep tables of the verified file by the schema adapter).
- S12: a 0.6 s smoothstep hand-off eases the static 40 px/cell camera into the auto-fit rule at 50.6 s (otherwise a hard jump inside the shot); the histogram (now `exhaustive.json -> results.k5.histogram_log_bins`, title `onset step, all 33,554,432 5x5 starts`, markers `this start` at 233,232 - the run on screen; the word `longest` is reserved for the card's `longest anywhere: 1,323,594` line - and `empty grid` at 9,977) is shown for the whole shot (fades in at 50.0); the pre-onset pad is the chaos cluster plus the Bb2 rub (A2 Bb2 Bb3 E4 F4).
- S12 camera (section 2.5 auto-fit): the bbox is fitted into the text-free stage band between the measured bottom of the full card B (~610 with its four lines) and the counter band (1125), `cells_across = clamp(1.3 * max(bw, bh * 1080/stage_h, 64), 64, 1080)` with the bbox centre on the band's centre, instead of the full frame; otherwise the run and the ant sit under the card, the counter or the histogram. After the onset frame the camera dives (1.6 s smoothstep) from that fit to 160 cells across with the ant sliding to the stage's far edge (see the S12 dive line of fixer round 3), so the ant and the fresh road stay on screen to the cut.
- S12 rate after the onset: one period (104 steps) per frame, S07's rate, instead of the constant pre-onset rate (233,232 / 4.8 s = 48,590 steps/s = 1,620 steps/frame would carry the road tip far off-screen within frames). The pre-onset rate and the 55.4 s onset frame are as specified; the counter freezes cream at 233,232 on the onset frame.
- section 2.6 fades: the 150 ms fade-in is offset by one frame (the card is at 22 % on its listed frame, full after 4 frames) and the fade-out mirrors it, so back-to-back cards overlap by one frame instead of leaving a card-less boundary frame (S05/S08/S12/S13/S14 boundaries).
- section 3.1 schedule: frame 600 shows the onset step (the S07 picture resumes from the frozen S06 frame), then 104 steps per frame, so S07 ends at onset+21,736 (frame 809) and S08 at onset+40,456 (frame 989); the table's S08 end value onset+40,560 is the value at 33.0 s (frame 990), from which S14 resumes. The turn strip never draws pre-onset turns (its window starts at the onset) and fades in over 150 ms from 20.0 s: the 208-tick window is full and pixel-identical from frame 602 and at full opacity from frame 604.
- S04: the counter is drawn at full opacity (fade-in only, no fade-out with the card) because it continues into S05.
- S10: tiles at y 452 / 832 / 1212 (rows 380 apart, storyboard 360 / 760 / 1160) because the two-line card B of the director's cut (band top 250) ends at ~446; the grid ends at 1574, inside the safe zone.
- S11 side bars: four 60 px rows (y 1290 / 1360 / 1430 / 1500): the random bar uses the storyboard's two Mono 40 fragments `random, up to 512x512:` / `4,730,000 / 4,730,000` on two rows; bars slide in at 48.0 / 48.25 / 48.5 s. Spec-inherent: the 5x5 bar is on screen 1.5 s and `Still a road.` (a deliberate slam at 55.4) 1.6 s, both under the 2.0 s rule of section 2.6.
- S14: each row is a pill card on the standard 85 % band (as S08 card B) because the 25 % highway ran under the sentences and citations; line 3 stacks in at +3.5 s = 70.5 s (not +4.0 = 71.0; the storyboard's 66.0 before the +5 s WALLS shift) so UNPROVEN holds >= 2.0 s at full opacity; the tone/pad drop-out moves with it.
- S13: the three mono lines share one size, the fit of the widest line (60 px with the verified 8-digit total), so the column stays aligned instead of only line 1 shrinking; y = 520/620/720 and 1000/1090 are the glyph-box tops.
- S14: the fine print of the UNPROVEN row reads `held in 41,835,387 runs. No proof.` (Mono 40, 819 px; the number is `total_runs_all_experiments`) instead of the storyboard's `every finite start, tested: yes`, which could be read as a claim that every finite start had been tested; the two PROVEN rows are unchanged; the amber underline pulses at 0.5 Hz under the sentence.
- S02/S03 audio: the pluck sounds at the listed step time (start of the 120 ms rotate phase) and the 3 ms click at the flip instant 120 ms later (the wipe), so turn and flip are two audible moments as the animation shows them; step 9's click (7.02 s) is emitted by S04.
- S02-S04 (director's cut): the ant trail is off whenever a cell is >= 40 px (the rules demo at 120 px and the first part of the S04 zoom-out: the cells themselves show the path; the blue dots read as marbles inside the big cells); the animated ant is drawn by the scene (chevron at a continuous angle) instead of render_run's fixed-heading marker; step 9's 450 ms animation finishes inside S04 (7.35 s) before the rate ramp fires step 10 (~7.4 s).
- S04 audio: the pluck -> oscillator crossover is where r(t) = 2·30^((t-7)/3) reaches 20 steps/s, i.e. 9.03 s (the storyboard's "~8.6 s" does not match its own rate formula); 0.5 s crossfade as specified. Drone E is added in S04 and S05 only (tension), plain drone from S07.
- S05 camera: cells across = max(ease-out 27 -> 90, bbox width / 0.6, bbox height x 0.5625 / 0.6) so the blob stays inside the middle 60 % of the frame even if it outgrows the nominal zoom (never triggers with the provisional onset).
- S06 card: centred at y = 1160 (over the lower half of the blob) instead of y = 880, because at y = 880 the band covers the ant and the pulsing 3-cell ring around it for the whole shot; the 2 % push-in is a zoom about the ant so it stays fixed on screen.
- S06 -> S07 and S07 24.0 s: the camera changes are 0.6 s smoothstep hand-offs (S06 push-in -> follow-cam, follow-cam -> pull-back) since neither is a listed hard cut.
- S07: the 60 % contrast cap is active for the whole shot including the onset frame 600 (`contrast_cap=True`: 104 steps/frame > 100, although the schedule holds the onset on frame 600 so `steps_drawn` is 0 there), as the section 2.5 rule demands; the storyboard's example list names S08 but not S07.
- S08 camera: the pull-back rule sets the zoom; the centre blends (smoothstep over the shot) from the midpoint rule toward a centre that puts the blob at screen (760, 700) at 33.0 s (mirrored by highway_sign), as the shot text asks; the ant leaves the frame at ~31.5 s ("a glowing dot leaving the lower-left"), so the road_backdrop for S09/S13 shows the blob upper-right and the road running out of the lower-left corner.
- S08 card B: the PROVEN pill sits above the sentence (2.6 allows "left of or above"): pill 298 px + 748 px line exceed the 880 px safe width; the band spans the whole safe width (x 80-960, centred on 520) so the pill can sit at x = 100.
- S15: the question band sits at top 280 (ends ~626; the storyboard's 300 left a 4 px gap) and the sub-lines band 24 px under it (top 650, ends ~903; the storyboard's 640-830 cannot hold three Regular 48 lines with the 28 px padding, 253 px) and the credit band is bottom-aligned to the safe zone, y 1437-1600 (its measured height is 163 px: Mono 40 + Mono 38 at 1.15 spacing plus 2 x 28 px, so the director's 1380 top would end at 1543, not 1490 - reviewer SCI-02), so that the hook blob can sit lower (reviewer V1): under the reframed hook camera the blob's top row is ~23 px below the sub-lines band and the road crosses x = 80 with its lower edge ~27 px above the credit band's rounded corner (both computed from the run's cells at 74.8 s, the end of the hook window), so neither band ever covers the blob, the road tip or the ant during 73.2-74.8 s. Sub-lines fade in at +0.6 s (73.6) and the credit at +1.0 s (unspecified). No counter (not in the S15 text list).
- S15 camera: identical to S01 until +2.0 s (frames 2190-2250 mirror S01 frames 0-60), then a 1 s smoothstep drift that carries the ant (which leaves the static hook frame at ~+1.5 s) along a Bezier through the lower-left, above the credit band, to the centre of the text-free band (y ~1170, between the sub-lines and the credit) while zooming 130 -> 90 cells; the camera follows the ant to +4.0 s and locks there so the 3,000 -> 8 -> 2 steps/s deceleration is seen as the ant hopping cell by cell while the plucks slow.
- S15 schedule: common.build_step_schedule keeps 3,000 steps/s to 79.0; the scene displays its own schedule (identical up to +4.0 s, so frames 2190-2250 mirror S01) with an exponential deceleration 3,000 -> 8 (+4.0-+4.4) -> 2 steps/s (+4.4-+5.5) whose last step fires exactly at +5.5 s (78.5) and is forced to be a left turn (schedule shifted by one step if needed) so the closing pluck is the A3; picture holds +5.5 to the end under the fade. Plucks take over from the tone when the rate drops below 20 steps/s (~+4.34 s, 0.5 s crossfade); pad and drone stop at +5.0.
- S01/S15 oscillator: -18 dBFS through S01; in S15 it rises to -14 dBFS over 73.5-74.5 (local +0.5 to +1.5 s of the 79 s timeline) under the A-major pad (levels for these shots are unspecified beyond the S01 crackle).
- S12 histogram (integrator): the marker labels `this start` / `empty grid` sit on a small dark band and each marker line gets a 7 px dark halo, because the secondary-grey `empty grid` marker and its label were illegible over the cream bars.
- section 4 master (integrator): the master is normalised by iterating on ffmpeg's ebur128 integrated measurement (RMS approximation only as a fallback); bare clicks / click clusters (S09), the S13 thuds and the S11 chord hit are 8-10 dB quieter than first tuned (their 3 ms bursts pushed the true peak to +2 dBTP); the master ends with a linked-stereo true-peak limiter (4x windowed-sinc oversampling, 1 ms blocks, 80 ms release) at -3.5 dBTP on the WAV because the 192 kbit/s AAC encode overshoots by 1-1.5 dB; the muxed file measures -14.1 LUFS integrated / -3.2 dBTP (fixer round 3 master; the earlier note said -14.3 / -2.6, reviewers T-03 / SCI-03).
- render.py: the text-shrink log is written next to the timeline (`<timeline>_shrinks.log`, e.g. `build/timeline_full_shrinks.log`) with a header naming the frame range, so partial renders never wipe the full film's log.
- section 4.1 / 4.4 audio (fixer): S11 micro-ticks are capped at 6, 6, 7 per frame (190/s, never above the 200/s cap); the master re-measures loudness and true peak after the last global trim; the turn-stream oscillator aborts if a read head runs past a run's turn array instead of holding the last value.
- S07 (fixer round 2): the counter, turn strip, bracket and `104 turns` label fade out over the last 150 ms (26.85-27.0 s, mirroring their fade-in) instead of vanishing on the S08 cut, since S07 -> S08 is not a listed hard cut and card A of S08 fades in over them.
- S07 audio (fixer round 2): the once-per-second kick is not emitted on frame 600, where the onset thump already lands (the two 48 Hz hits would stack); kicks run 630, 660, ..., 780.
- S10 tile label (fixer round 2): the two-line `road at / step N` band is placed off the road: at the tile's top edge when the road exits downward (else the bottom edge), pushed toward the side opposite the road's x direction (direction derived from the run's own displacement, as the certification is); the storyboard's "inside the tile's bottom edge" put the band on the road of the two -y tiles.
- S12 histogram axis (fixer round 2): a tick label is centred on its tick but clamped inside the chart box (x 100-940), and a label that would then collide with its neighbour is dropped while the tick mark stays; with LONGEST_ONSET = 1,323,594 the `1,000,000` label is dropped (it overran the safe zone to x = 975) and the amber `this start` marker labels that region.
- S14 camera (fixer round 2): the follow-cam anchors the ant at screen (W/2 + sx*180, H/2 - sy*540) = (360, 1500) for the -x,-y highway (below row 3, whose band ends at ~1382) instead of the frame centre (which is under row 2's band for the whole shot), still 135 cells across; the road then crosses the frame diagonally behind the rows.
- S15 fade (fixer round 2): the fade to `#0B0D12` runs from frame 2355 (78.5 s) to the last frame 2369 inclusive, so the final frame is exactly the plain colour.
- section 5 (fixer round 2 + director's cut): each mosaic tile's re-derived onset is asserted against the 4x4 per-config records (`onset` for its cfg index, `status == 0`) as well as the 3.3 table; the records are read from the committed `research/results/exhaustive_k4_records.npz` (packed from the gitignored CSV by `make_k4_records_npz.py`, encoding in the README) with the CSV as the fallback, so a fresh clone renders without regenerating the CSV.

- Appendix A / section 1 (director's cut): the WALLS insert is IN (obstacle data exists): 5.0 s between S12 and S13, S13 62.0-67.0, S14 67.0-73.0, S15 73.0-79.0, total 79.0 s = 2370 frames; the audio timeline extends with it. Its numbers are `adversarial.obstacle_attack.{hits, hits_rebuilt_highway, placements}` (616 / 616 / 1,512), mapped to the `obstacle` object by the schema adapter.
- WALLS placement: ONE real line of `research/adversarial/out/attack_a.jsonl` - shape 3x3, anchor (-108, -84), cells (-108..-106, -84..-82), contact step 14,600, onset 18,043, extra chaos 3,443 (median 5,012), rebuilt direction -x,+y (a right angle away from the empty grid's -x,-y road), 21 periods ahead of the blob so a stretch of old road is visible between blob and block - recorded in `common.WALLS_PLACEMENT`; the renderer simulates the empty grid plus those nine cells (4096 grid: the rebuilt road runs ~850 cells), re-derives the contact step and `core.find_onset` and asserts both against the record, and checks the record against the jsonl line when that gitignored file exists.
- WALLS rate: 800 steps/s (Appendix A) before the impact; after it the rate doubles every 20 frames from 26.7 steps/frame (capped at 3,000 steps/frame) - the cap never binds, the rate reaches 1,648 steps/frame at the cut - because 3,443 chaos steps must stay visible for ~1.6 s and the ~850-cell rebuilt road must still fit the 1,080-cell pull-back at the cut; the re-derived onset lands on local frame 79 (59.63 s), where the tone returns an octave up (read heads at 2x = 440 Hz, `rate_mult` in the osc state).
- WALLS block: the nine block cells are painted as empty grid until +0.1 s (the director's +0.3 left the wall 0.7 s on screen before the impact, reviewer V5) and then shown as they are (cream = old ink) with an amber outline; from the impact frame the outline is blue-white (190, 215, 255) on a 1 px dark halo, drawn above the age tint to the cut, because the amber outline vanished inside the amber chaos (reviewer V4); the block is part of the simulation from step 0 (as in attack A), which is invisible because the empty-grid blob never reaches it before the contact (asserted: contact step > empty-grid onset). A 2-frame screen shake (+-14 px camera offset) marks its appearance, with a filtered thud (unspecified in the director's audio note; the thump is on the impact).
- WALLS camera: pre-impact follow-cam, 40 cells across (27 px/cell, the 3x3 block an 81 px square - the director's ~60 gave a 54 px block, reviewer V5), the ant 12 cells from the centre on the blob side (highway_sign) so the block ahead sits near the centre; the post-impact fit never zooms closer than 60 cells; at the impact a 0.6 s smoothstep hand-off into a bbox fit (cells_across = clamp(1.3 * max(bw, bh * 1080 / 1400), 60, 1080) into the stage below card B, 0.3 s lag fed the bbox 9 frames ahead so the accelerating road tip stays inside): the bbox is the chaos region (cells modified since the contact plus the block) until +1.6 s, widening (smoothstep to +2.6 s) to everything modified - blob, old road, block, new road - so the pull-back reveals the old road and the blob after the chaos has boiled. Because the fit centres the bbox whose leading edge is the road tip, once the 1,080-cell cap binds (local frame ~142; the bbox is ~1,000 cells wide at the cut) the ant ran off the left edge in the last four frames: the post-impact camera is therefore shifted so the ant's screen point passes through a softplus floor (`_walls_keep_ant_inside`: 60 px margin, 60 px knee, x against the frame edges and y against the stage band), which eases the ant to a halt 80 px inside the left edge on the last frame (1859) while the blob's far edge stays on screen (x ~1,076) and the block and both roads remain visible.
- WALLS text: card A `A wall in its path?` (Regular 56, +0.0-+2.5, band top 300), card B `616 hits. 616 new roads.` (Regular 56) + `1,512 walls on or beside the road` (Mono 40, secondary; the director's `placed on the road` was wrong for the 896 placements at lateral offsets beside the highway strip that never touched the ant's path - reviewer SCI-01; `attack_a_summary.json`: 616 hit, 896 missed with the empty-grid onset), +2.5 to the cut; Appendix A's `A mess... then a new road.` is replaced by the director's numbers line. Pad: highway before the impact, the chaos cluster with rising cutoff to the onset, highway + shimmer after.
- S10 text (director's cut): card A is the single first-person line `I tested it 41,835,387 times.` (Regular 56, 797 px; the number is `total_runs_all_experiments`), card B `Every one built a road.` / `Nobody can prove it always will.` (Regular 52); Claude speaks in first person exactly once (S12's card A is third person).
- S01 / S15 hook framing (director's cut + reviewer V1): 130 cells across (8.3 px/cell) with the blob centre (centroid of the cells modified up to the onset) at screen (590, 1090) - mirrored by highway_sign - instead of 180 cells centred at (-25, -25): the road erupts from the blob and reaches the lower-left frame edge at ~1.4 s (the ant leaves the frame at ~1.4 s); (590, 1090) rather than the director's (640, 1000) because the blob's top rows sat under the S15 sub-lines band at y 1010 (reviewer V1) and, once lowered, the road's lower edge would have crossed x = 80 inside the credit band unless the blob also moved 50 px left (the crossing height follows the blob's x).
- S12 card B secondary line (director's cut): `longest anywhere: 1,323,594 steps (512x512 box)` measures 1,132 px at Mono 40, so it is two Mono 40 lines, `longest anywhere: 1,323,594 steps` / `(512x512 box)` (the box size is `random.longest_config.k`, parsed from `random.longest_config_summary`); the longest-anywhere run is still re-simulated (4,000 tail steps) and its onset asserted, as section 5 asks, although only its number is shown.
- section 2.3 text bands (reviewer V3-01): every band rectangle (cards, counters, labels, side bars) is clamped to x 80..960 by `common.clamp_band_x` - the padding gives way, the text (always <= 880 px) stays centred on x = 520 - so a full-width line no longer pushes its band to x 52..988.
- S12 chart title: `onset step, all 33,554,432 5x5 starts` measures 802 px at Mono 36, wider than the 792 px plot area, so the title may use the chart box's full width minus 16 px (824 px) and stays at 36 px.
- S12 static beat (reviewer V2): the 11-cell pattern is shown at 40 px/cell (a 200 px square, hairline grid on) instead of the director's ~20 px, and the ant's halo is scaled to 25 % of its usual reach (`render_run(ant_glow=)`) during 50.0-50.6 so the cells next to the ant stay readable; the halo eases back to normal over the 0.6 s hand-off into the auto-fit (27 -> 64 cells across).
- S12 dive (reviewer V7): the 1.6 s dive after the onset slides the ant to `_s12_dive_target` - the far edge of the stage band (40 px above the counter band for this run, whose highway heads +x,-y, derived from the run) with the road's centre line aimed at the frame corner on the road's side at the stage's near edge - instead of the stage centre, so the fresh road leaves the frame at x < 80 before it could pass under card B.
- S05 trail (reviewer V6): the ant trail fades in over 40 -> 30 px/cell (`TRAIL_FADE_PX`, ~0.3 s of the S05 zoom) instead of switching on hard when the cell size crosses the 40 px director's threshold.
- S04 audio (reviewer T-01): plucks are at full gain up to 4 steps/s and lose 3.5 dB per doubling of the instantaneous rate r(t) = 2 * 30^((t-7)/3) above it (-5.1 dB at 11/s, -8.1 dB at 20/s, then the 0.5 s crossfade to -30 dB as before), so the 350 ms plucks stacking six deep at 10-20/s no longer form the loudest passage of the film.
- tooling (reviewer T-04 / T-05): `core.ffprobe_exe()` looks for an ffprobe next to the bundled ffmpeg by basename (the old `replace('ffmpeg', 'ffprobe')` also rewrote the `imageio_ffmpeg` directory) and then on PATH; the mosaic assertion messages name the records file actually loaded (`rec['source']`: the npz or the CSV).
